#ifndef LOSOWY_HPP
#define LOSOWY_HPP
//Bierze rozmiar przed wykonywaniem testów aby mozna bylo losowac priorytet
void ustawRozmiar(int r);


// Losuje jedna z liczb w strukturze
int losujPozycje();

// Losuje wartosc dla struktury -> Priorytwtów jest 2*size
int losujWartosc();
#endif